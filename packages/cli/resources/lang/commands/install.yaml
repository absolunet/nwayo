description:
  en: 'Install dependencies ex:[{{command}}]'
  fr: 'Installer les dépendances ex:[{{command}}]'

parameters:
  scope:
    en: The installation scope.
    fr: Le "scope" de l'installation.

flags:
  force:
    en: The "install workflow" force flag.
    fr: L'indicateur pour forcer l'"install workflow".
