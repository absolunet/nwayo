#es     = require 'event-stream'
gulp = require 'gulp'

path =
	build:      '../build'
	common:     '../common'
	components: '../components'
	sources:    '../{common,components}'
	assets:     '../{common,components}/assets'
