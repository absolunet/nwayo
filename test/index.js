//--------------------------------------------------------
//-- Tester
//--------------------------------------------------------
'use strict';

const { tester } = require('@absolunet/tester');

tester.init({
	repositoryType: 'multi-package',
	packageType:    'simple'
});
